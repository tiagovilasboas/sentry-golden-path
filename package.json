{
  "name": "sentry-golden-path",
  "version": "1.0.0",
  "private": true,
  "description": "Staff-grade Sentry golden path — conservative sampling, Web Vitals, production-safe defaults.",
  "license": "MIT",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "check": "node scripts/check.mjs",
    "typecheck": "tsc --noEmit -p examples/tsconfig.json",
    "test": "node scripts/check.mjs && tsc --noEmit -p examples/tsconfig.json"
  },
  "devDependencies": {
    "@sentry/react": "^10.32.1",
    "@sentry/vue": "^10.32.1",
    "typescript": "^5.9.2",
    "vue": "^3.5.21"
  }
}
