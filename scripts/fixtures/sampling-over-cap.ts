/**
 * Anti-fixture. EXPECTED FAIL.
 *
 * Wizard / first-run `tracesSampleRate: 1.0` exceeds this kit's 0.05 cap.
 * Not a copy-paste init. `npm test` asserts the Staff check rejects this file.
 */
export const tracesSampleRate = 1.0;
